#include <iostream>
using namespace std;


int main()
{
    // Variables are stored here.
    
    string _calculator  = "Hesap makinesi";
    float _firstNumber, _secondNumber;
    char _symbol;

    // inputs requested from us.
    cout << "Sayi giriniz" << endl;
    cin >> _firstNumber;

    cout << "Işlem türü seçiniz" << endl;
    cin >> _symbol;

    cout << "Sayi giriniz" << endl;
    cin >> _secondNumber;

    // With the switch case structure, we determine which operation we want and carry out the desired operation accordingly.
    switch (_symbol)
    {
    case 
        break;
    }
    return 0;
}